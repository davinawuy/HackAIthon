import { ExploreClient } from "@/components/sections/ExploreClient";
import { SectionWrapper } from "@/components/ui/SectionWrapper";

export default function ExplorePage() {
  return (
    <SectionWrapper className="py-6 sm:py-8">
      <ExploreClient />
    </SectionWrapper>
  );
}
